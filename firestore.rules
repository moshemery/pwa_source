rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /groups/{groupId} {
      // Anyone with the group ID can read/write.
      // The group ID acts as a shared secret — keep it private.
      allow read, write: if true;
    }
  }
}
